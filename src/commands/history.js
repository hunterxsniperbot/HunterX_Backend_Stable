/**
 * Módulo de comando: ${cmd}
 * Añade aquí la implementación de /${cmd}
 */
module.exports = (bot) => {
  bot.command('${cmd}', ctx => {
    ctx.reply('Comando /${cmd} aún no implementado.');
  });
};
